for $tuple in join(
(
  for $s in document("file:/Users/tony/Desktop/milestone2/data/j_caesar.xml")//SPEAKER,
      $stxt in $s/text()
  where $stxt eq "CAESAR"
  return <tuple> <stxt>{$stxt}</stxt>, <s>{$s}</s></tuple>
),
(
  for $a in document("file:/Users/tony/Desktop/milestone2/data/j_caesar.xml")//ACT,
      $sp in $a//SPEAKER
  return <tuple> <sp>{$sp}</sp>, <a>{$a}</a ></tuple>
),
  [s], [sp]
)
return <act>{$tuple/a/*/TITLE/text()}</act>